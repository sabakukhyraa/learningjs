function isim(...list) {
  var name = prompt("Bir değer giriniz","lütfen sadece kelime giriniz..")
  return name
}

const birthday = (name) => "Happy Birthday " + name + "!";

console.log(birthday("Alex"))



alert(`Hoş geldin ${isim()}!`)